@page "/Settings"

//the header and footer are store in text files in the wwwroot/receiptSettings directory
//we need header.txt file editor, freeform the user can type whatever they want.
//we need footer.txt file editor, freeform the user can type whatever they want.
//the user can the set sales tax rate (no tax chart support), the sales tax should be read at startup just like the header and footer files.
//all this in the same page


<h3>Settings</h3>

@code {

}
